import { Component } from '@angular/core';

@Component({
  selector: 'sys-table',
  templateUrl: './sys-table.component.html',
  styleUrls: ['./sys-table.component.scss']
})
export class SysTableComponent {
  // addHeaderColumn(columnName: string) {
  //   this.headers.toArray().forEach(header => header.addColumn(columnName));
  // }
}
